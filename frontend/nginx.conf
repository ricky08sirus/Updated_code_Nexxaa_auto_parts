# server {
#     listen 80;
#     server_name nexxaauto.com www.nexxaauto.com 74.208.158.237 ;
#     root /usr/share/nginx/html;
#     index index.html;

#     # Serve pre-rendered HTML files from folders
#     location / {
#         try_files $uri $uri/index.html $uri/ /index.html;
#     }

#     # Gzip compression
#     gzip on;
#     gzip_vary on;
#     gzip_min_length 1024;
#     gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

#     # Cache static assets
#     # location /static/ {
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|webp|woff|woff2|ttf|eot)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }

#     # React Router - all routes go to index.html
#     location / {
#         try_files $uri $uri/ /index.html;
#         add_header Cache-Control "no-cache";
#     }

#     # Security headers
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
# }


server {
    listen 80;
    server_name nexxaauto.com www.nexxaauto.com 74.208.158.237;
    root /usr/share/nginx/html;
    index index.html;

    # Serve pre-rendered HTML files from folders
    location / {
        try_files $uri $uri/index.html $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|webp|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;
}
# Security headers - KEEP THESE! ✅
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
# server {
#     listen 80;
#     server_name nexxaauto.com www.nexxaauto.com 74.208.158.237 localhost 127.0.0.1;
#     root /usr/share/nginx/html;
#     index index.html;

#     # Cache static assets (this must come BEFORE the main location block)
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|webp|woff|woff2|ttf|eot)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }

#     # React Router - all routes go to index.html (SINGLE location / block)
#     location / {
#         try_files $uri $uri/ /index.html;
#         add_header Cache-Control "no-cache";
#     }

#     # Gzip compression
#     gzip on;
#     gzip_vary on;
#     gzip_min_length 1024;
#     gzip_comp_level 6;
#     gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json image/svg+xml;

#     # Security headers
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
# }